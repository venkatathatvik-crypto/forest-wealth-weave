# Loaded by Vite in dev mode (`vite dev`). Frontend (5173) and backend (8080)
# are different origins here, so use the full backend URL. CORS on the backend
# already allows http://localhost:5173.
VITE_API_BASE_URL=http://localhost:8080/gold-emi-api
